- hosts: {{ target_host }}
  remote_user: {{ target_user }}

  roles:
  - quarry

  tasks:
  - name: Delete a snapshot
    quarry_snapshot:
      backend: {{ backend }}
      config: {{ backend_config }}
      state: absent
      id: {{ params.id }}
